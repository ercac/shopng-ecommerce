# ============================================================
# ENVIRONMENT VARIABLES
# ============================================================
# These keep sensitive data out of your source code.
# NEVER commit .env files to git!
# ============================================================

# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/shopng

# JWT secret key — used to sign and verify authentication tokens
# In production, use a long random string!
JWT_SECRET=shopng-super-secret-key-change-me-in-production

# Server port
PORT=3000
